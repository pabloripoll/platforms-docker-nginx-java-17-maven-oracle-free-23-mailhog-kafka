services:

  kafka-ui:
    ports:
      - ${COMPOSE_HOST}:${COMPOSE_APP_PORT}:8080
    networks:
      net: {}

networks:
  net:
    driver: bridge
    external: true
    name: ${COMPOSE_LEAD}-${COMPOSE_CNET}
  default:
    external: true
    name: ${COMPOSE_LEAD}-${COMPOSE_CNET}